# Web 前端

短信触达平台前端位于 `apps/web`，使用 React、TypeScript、Vite。

## 开发

从仓库根目录启动完整开发环境：

```bash
npm run dev
```

只启动前端：

```bash
npm run dev:web
```

访问：

```text
http://127.0.0.1:5173
```

## UI 规范

前端页面风格、组件、弹窗、查询条件、列表分页等规范见：

[UI 风格与交互规范](docs/ui-style-spec.md)
